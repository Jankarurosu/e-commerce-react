import './Popular.css'

import data_product from '../assets/data'
import { Item } from '../item/Item'

export const Popular = () => {
    return (
        <div className="popular">
            <h1>POPULAR IN WOMEN</h1>
            <hr />
            <div className="popular-item">
                {data_product.map((item, i) => {
                    return <Item
                        key={i}
                        id={item.id}
                        name={item.name}
                        image={item.image}
                        new_price={item.new_price}
                        old_price={item.old_price} />
                })}
            </div>
        </div>
    )
}


Hero.css

.popular {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
    margin-bottom: 100px;
}

.popular h1 {
    color: #171717;
    font-size: 50px;
    font-weight: 600;
}

.popular hr {
    width: 200px;
    height: 5px;
    border-radius: 10px;
    background: #252525;
}

.popular-item {
    margin-top: 50px;
    display: flex;
    gap: 30px;
}

Actua como un programador de front end profesional y eficiente, y quiero que completes la siguiente tarea, tomando en cuenta la estructura del HTML y del CSS, realiza el codigo para el modo responsive de este componente Hero para que se adapte a varios tipos de pantallas y manteniendo como prioridad una buena experiencia de usuario esto quiere decir que todo tiene que mantener un orden al entrar al modo responsive.

Especificacmente lo que se requiere para un buen diseño responsive es lo siguiente:

1. Desde 1250px hasta 960px, las letras junto con el boton y la imagen tienen que disminuir su tamaño manteniendo la misma estructura.

2. Desde 960px hasta menos, las letras junto con el boton se ubicaran en la parte superior de la pantalla y la imagen abajo de estas, cada uno va disminuyendo su tamaño de acuerdo al tamaño, siempre manteniendo el orden.